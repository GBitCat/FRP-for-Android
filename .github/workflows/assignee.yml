name: Assignee
on:
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  assignee:
    runs-on: ubuntu-latest
    steps:
      - uses: pozil/auto-assign-issue@v1
        with:
          assignees: gbitcat
          numOfAssignee: 1
