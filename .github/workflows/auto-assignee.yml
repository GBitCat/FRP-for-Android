name: Auto Assignee
on:
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  auto-assignee:
    runs-on: ubuntu-latest
    steps:
      - uses: pozil/auto-assign-issue@v4
        with:
          assignees: gbitcat
          numOfAssignee: 1
